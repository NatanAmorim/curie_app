import 'package:flutter/material.dart';

class RouteTestView extends StatelessWidget {
  const RouteTestView({super.key});

  static const routeName = '/test/:page';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
