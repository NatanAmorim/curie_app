class Times {
  static const Duration slower = const Duration(milliseconds: 1000);
  static const Duration slow = const Duration(milliseconds: 700);
  static const Duration fast = const Duration(milliseconds: 250);
  static const Duration medium = const Duration(milliseconds: 350);
  static const Duration fastest = const Duration(milliseconds: 150);
}
